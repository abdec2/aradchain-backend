'use strict';

/**
 * vendor-verification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vendor-verification.vendor-verification');
